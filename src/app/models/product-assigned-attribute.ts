/**
 * This is a product attribute that is actually defined
 * on a given product.
 */
export interface IProductAssignedAttribute {
  attribute: string; // href to the product attribute.
  value: any;
}
