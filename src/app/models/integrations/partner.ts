export interface IPartner {
  name: string;
  partner: string;
  slug: string;
  clientId: number;
  companyId: number;
  categoryId: number;
  customerId: number;
  access: string;
  isActive: boolean;
}
