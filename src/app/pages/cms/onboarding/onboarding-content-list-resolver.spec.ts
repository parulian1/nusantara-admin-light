import { OnboardingContentListResolver } from './onboarding-content-list-resolver';

describe('OnboardingContentListResolver', () => {
  it('should create an instance', () => {
    expect(new OnboardingContentListResolver()).toBeTruthy();
  });
});
